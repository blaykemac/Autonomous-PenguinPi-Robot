Change directory into the M4 folder.

To run semi auto using cmd for waypoints, use command:
python3 delivery_semiauto.py --map PATH/TO/MAP

To run with GUI instead, run command:
python3 delivery_semiauto.py --map PATH/TO/MAP --gui
